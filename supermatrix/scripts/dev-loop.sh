#!/bin/zsh
# Retired: localwatch is the only supported SuperMatrix supervisor. Keeping a
# fail-closed stub prevents old docs, shell history, or automation from reviving
# the destructive takeover loop.

echo "[maintenance-denied] dev-loop.sh is retired; use the codexroot maintenance gate and localwatch." >&2
exit 2
